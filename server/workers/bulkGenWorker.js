// BulkGenWorker disabled: BullMQ and Redis connection removed to avoid Upstash request exhaustion.
// Content generation should be handled directly via API/service, not through this worker.

// If you want to re-enable bulk generation with queue/Redis, restore the original code.
